% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.pgplsim}
\alias{predict.pgplsim}
\title{Predict from a fitted pgplsim model}
\usage{
\method{predict}{pgplsim}(
  object,
  newdata = NULL,
  newX = NULL,
  newZ = NULL,
  type = c("response", "link", "index", "smooth", "linear"),
  offset = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted object of class \code{"pgplsim"}.}

\item{newdata}{Optional data frame containing variables used in the original
model. If \code{NULL}, prediction uses \code{newX}/\code{newZ} when supplied; otherwise it
uses the original design matrices stored in \code{object}.}

\item{newX}{Optional matrix of linear-component covariates for low-level
matrix-based prediction. Do not supply together with \code{newdata}.}

\item{newZ}{Optional matrix of single-index covariates for low-level
matrix-based prediction. Do not supply together with \code{newdata}.}

\item{type}{Character string specifying the prediction scale. One of
\code{"response"}, \code{"link"}, \code{"index"}, \code{"smooth"}, or \code{"linear"}.}

\item{offset}{Optional numeric offset on the linear-predictor scale for
Poisson prediction. A scalar is recycled to all prediction observations.
For an exposure such as population or person-time, supply its logarithm,
for example \code{offset = log(exposure)}. Offsets are not supported for
binomial models.}

\item{...}{Additional arguments passed to the internal matrix prediction
engine.}
}
\value{
A numeric vector of predictions. For \code{newdata} predictions, rows
containing missing required covariates (or a missing Poisson offset) are
returned as \code{NA} while preserving the row order of \code{newdata}.
}
\description{
Generate predictions from a fitted generalized partially linear
single-index model.
}
\details{
For models fitted with the formula interface, new observations should
normally be supplied through \code{newdata}. The linear and single-index design
matrices are then reconstructed automatically from the formulas stored in
the fitted model.

The \code{newX} and \code{newZ} arguments provide a low-level matrix interface.
For ordinary use, \code{newdata} is recommended. The matrix arguments should
not be supplied together with \code{newdata}.
}
