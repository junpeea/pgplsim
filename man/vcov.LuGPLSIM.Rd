% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov.R
\name{vcov.LuGPLSIM}
\alias{vcov.LuGPLSIM}
\title{Variance-Covariance Matrix for a LuGPLSIM Model}
\usage{
\method{vcov}{LuGPLSIM}(
  object,
  type = c("model", "sandwich", "leverage", "finite", "bootstrap"),
  component = c("parameters", "alpha", "beta", "theta", "gamma", "full"),
  bootstrap = NULL,
  finite_df = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted object of class \code{"LuGPLSIM"}.}

\item{type}{Variance estimator. One of \code{"model"}, \code{"sandwich"},
\code{"leverage"}, \code{"finite"}, or \code{"bootstrap"}.}

\item{component}{Parameter block to return. One of \code{"parameters"},
\code{"alpha"}, \code{"beta"}, \code{"theta"}, \code{"gamma"}, or \code{"full"}.}

\item{bootstrap}{Optional object returned by
\code{\link[=bootstrap_LuGPLSIM]{bootstrap_LuGPLSIM()}}. Required when \code{type = "bootstrap"} unless
a bootstrap result is stored in \code{object$bootstrap}.}

\item{finite_df}{Optional effective parameter count used for the
finite-sample correction. The default is the dimension of the
unconstrained parameter vector.}

\item{...}{Additional arguments, currently ignored.}
}
\value{
A variance-covariance matrix.
}
\description{
Extracts model-based, sandwich, leverage-adjusted,
finite-sample-corrected, or bootstrap variance estimates
for a fitted LuGPLSIM model.
}
\details{
The internal unconstrained parameter ordering is

\deqn{
  (\theta^\top, \beta^\top, \gamma^\top)^\top,
}

where

\deqn{
  \alpha(\theta)
  =
  \frac{(1,\theta^\top)^\top}
       {\sqrt{1+\|\theta\|^2}}.
}

The default returned matrix corresponds to the scientifically
interpretable parameter vector

\deqn{
  (\alpha^\top,\beta^\top)^\top.
}
}
