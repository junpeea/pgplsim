% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_pgplsim}
\alias{bootstrap_pgplsim}
\title{Bootstrap Inference for pgplsim}
\usage{
bootstrap_pgplsim(
  object,
  B = 500L,
  y = NULL,
  X = NULL,
  Z = NULL,
  offset = NULL,
  method = c("case", "stratified"),
  seed = NULL,
  max_attempts = max(2L * B, B + 50L),
  parallel = FALSE,
  ncores = 2L,
  trace = interactive(),
  keep_fits = FALSE,
  use_starting_lambda = TRUE,
  ...
)
}
\arguments{
\item{object}{A fitted object of class \code{"pgplsim"}.}

\item{B}{Target number of successful bootstrap replications. Must be an
integer greater than or equal to 2. Additional fits may be attempted
when individual bootstrap fits fail to converge.}

\item{y}{Optional original response vector. If omitted,
\code{object$y} is used.}

\item{X}{Optional original matrix of linear covariates. If omitted,
\code{object$X} is used.}

\item{Z}{Optional original matrix of single-index covariates. If omitted,
\code{object$Z} is used.}

\item{offset}{Optional original offset vector. If omitted,
\code{object$offset} is used.}

\item{method}{Bootstrap resampling method. \code{"case"} samples observations
from the full data. \code{"stratified"} samples separately within outcome
groups and is intended primarily for binomial models.}

\item{seed}{Optional random-number seed.}

\item{max_attempts}{Maximum number of attempted fits. Failed bootstrap
fits are replaced until \code{B} successful fits are obtained or this limit
is reached.}

\item{parallel}{Logical; use parallel processing when \code{TRUE}.}

\item{ncores}{Number of worker processes used when \code{parallel = TRUE}.
The default is 2. During CRAN checks, the number of workers is capped
at 2.}

\item{trace}{Logical; print progress information.}

\item{keep_fits}{Logical; retain successful fitted model objects.}

\item{use_starting_lambda}{Logical; initialize each bootstrap fit using
the smoothing parameter from the original fitted model.}

\item{...}{Additional arguments passed to \code{\link[=pgplsim]{pgplsim()}}. Arguments supplied
here override corresponding settings recovered from the original fit.}
}
\value{
An object of class \code{"bootstrap.pgplsim"} containing the original
parameter estimates, bootstrap replicate estimates, bootstrap
variance-covariance matrices and standard errors, the number of attempted
and failed fits, failure messages, and optionally the successful fitted
model objects when \code{keep_fits = TRUE}.
}
\description{
Performs a nonparametric case bootstrap for a fitted \code{pgplsim}
model. For binomial models, a stratified bootstrap can be used
to preserve the observed number of observations in each outcome
category.
}
\details{
Each bootstrap data set is refitted using \code{\link[=pgplsim]{pgplsim()}}.
}
