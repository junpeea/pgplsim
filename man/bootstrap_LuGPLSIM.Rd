% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_LuGPLSIM.R
\name{bootstrap_LuGPLSIM}
\alias{bootstrap_LuGPLSIM}
\title{Bootstrap Inference for LuGPLSIM}
\usage{
bootstrap_LuGPLSIM(
  object,
  B = 500,
  y = NULL,
  X = NULL,
  Z = NULL,
  offset = NULL,
  method = c("case", "stratified"),
  seed = NULL,
  max_attempts = max(2 * B, B + 50),
  parallel = FALSE,
  ncores = max(1L, parallel::detectCores(logical = FALSE) - 1L),
  trace = interactive(),
  keep_fits = FALSE,
  use_starting_lambda = TRUE,
  ...
)
}
\arguments{
\item{object}{A fitted object of class \code{"LuGPLSIM"}.}

\item{B}{Number of successful bootstrap replications.}

\item{y}{Optional original response vector. If omitted,
\code{object$y} is used.}

\item{X}{Optional original matrix of linear covariates. If omitted,
\code{object$X} is used.}

\item{Z}{Optional original matrix of single-index covariates. If omitted,
\code{object$Z} is used.}

\item{offset}{Optional original Poisson exposure vector. If omitted,
\code{object$offset} is used.}

\item{method}{Bootstrap resampling method. \code{"case"} samples observations
from the full data. \code{"stratified"} samples separately within outcome
groups and is intended primarily for binomial models.}

\item{seed}{Optional random-number seed.}

\item{max_attempts}{Maximum number of attempted fits. This permits failed
bootstrap fits to be replaced until \code{B} successful fits are obtained.}

\item{parallel}{Logical; use parallel processing when \code{TRUE}.}

\item{ncores}{Number of worker processes.}

\item{trace}{Logical; print progress information.}

\item{keep_fits}{Logical; retain successful fitted model objects.}

\item{use_starting_lambda}{Logical; initialize each bootstrap fit using
the smoothing parameter from the original fitted model.}

\item{...}{Additional arguments passed to \code{\link[=LuGPLSIM]{LuGPLSIM()}}. Arguments supplied
here override corresponding settings recovered from the original fit.}
}
\value{
An object of class \code{"bootstrap.LuGPLSIM"} containing bootstrap
estimates, covariance matrices, standard errors, and convergence
information.
}
\description{
Performs a nonparametric case bootstrap for a fitted LuGPLSIM
model. For binomial models, a stratified bootstrap can be used
to preserve the observed number of observations in each outcome
category.
}
\details{
Each bootstrap data set is refitted using \code{\link[=LuGPLSIM]{LuGPLSIM()}}.
}
